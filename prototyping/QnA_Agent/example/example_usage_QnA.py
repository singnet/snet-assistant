# # import readjson

import src
import os
import sys


fpath = os.path.join(os.path.dirname(os.path.dirname(__file__)), 'src')

sys.path.append(fpath)
print(sys.path)

# # # question
# # question = """how To get the metamask plugin install?"""

# # print(src.respond_to_context(question))
